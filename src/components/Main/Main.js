import { Routes, Route } from 'react-router-dom';
import Login from '../Login/Login';

function Main() {

    return (
        <div>
            <Routes>
                
            </Routes>
        </div>
    );
}

export default Main;